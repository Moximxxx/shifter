---
description: Build the project (tsc + vite)
agent: builder
---

Run `bun run build` to compile TypeScript and bundle with Vite.
Report any build errors with details.
If the build succeeds, confirm the output files are in the dist/ directory.
