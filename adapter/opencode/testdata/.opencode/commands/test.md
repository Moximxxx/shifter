---
description: Run test suite with vitest
agent: builder
---

Run the full test suite using `bun run test`.
Show any test failures with their file paths and line numbers.
For each failure, suggest the likely root cause and a fix approach.
